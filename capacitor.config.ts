import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'po-sync-getting-started',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
